import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Si tu utilises un fichier CSS
import KarenDashboard from './KarenDashboard';

ReactDOM.render(
  <React.StrictMode>
    <KarenDashboard />
  </React.StrictMode>,
  document.getElementById('root')
);
